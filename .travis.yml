language: node_js
node_js:
  - "stable"
addons:
  apt:
    packages:
    - libjpeg-dev
    - libpng-dev
    - libpng16-16

install:
  - npm install
  - npm install -g gulp-cli

script:
  - gulp build

deploy:
  provider: firebase
  token:
    secure: $FIREBASE_CI_TOKEN
  on:
    branch: master
